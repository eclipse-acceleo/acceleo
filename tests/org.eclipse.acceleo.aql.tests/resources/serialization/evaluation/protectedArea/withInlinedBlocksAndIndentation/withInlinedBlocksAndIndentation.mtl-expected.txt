[module withInlinedBlocksAndIndentation('http://www.eclipse.org/emf/2002/Ecore')/]

[comment @main /]
[template public myTemplate(myParam : ecore::EPackage)]
  [file (myParam.name + '.txt', overwrite)]
    some text
      [protected (myParam.name)]
        [myParam.name/]
        [if (true)]inlined then block[else]inlined else block[/if]
        [if (true)]['inlined then block'/][else]['inlined else block'/][/if]
        [if (false)]inlined then block[else]inlined else block[/if]
        [if (false)]['inlined then block'/][else]['inlined else block'/][/if]
        [if (true)]
          then block
        [else]
          else block
        [/if]
        [if (true)]
          ['then block'/]
        [else]
          ['else block'/]
        [/if]
        [if (false)]
          then block
        [else]
          else block
        [/if]
        [if (false)]
          ['then block'/]
        [else]
          ['else block'/]
        [/if]
        [myParam.name/]
        [for (v | Sequence{1, 2, 3})]v[/for]
        [for (v | Sequence{1, 2, 3})][v/][/for]
        [for (v | Sequence{1, 2, 3})]
          v
        [/for]
        [for (v | Sequence{1, 2, 3})]
          [v/]
        [/for]
        [let myVariable : ecore::EClassifier = myParam.eClassifiers->first()]let block[/let]
        [let myVariable : ecore::EClassifier = myParam.eClassifiers->first()]['let block'/][/let]
        [let myVariable : ecore::EClassifier = myParam.eClassifiers->first()]
          let block
        [/let]
        [let myVariable : ecore::EClassifier = myParam.eClassifiers->first()]
          ['let block'/]
        [/let]
      [/protected]
    some text
  [/file]
[/template]