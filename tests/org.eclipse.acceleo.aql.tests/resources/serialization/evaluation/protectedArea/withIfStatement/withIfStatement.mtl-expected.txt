[module withIfStatement('http://www.eclipse.org/emf/2002/Ecore')/]

[comment @main /]
[template public myTemplate(myParam : ecore::EPackage)]
  [file (myParam.name + '.txt', overwrite)]
    some text
      [protected (myParam.name)]
        [myParam.name/]
        [if (true)]inlined then block[else]inlined else block[/if]
        [if (false)]inlined then block[else]inlined else block[/if]
        [if (true)]
          then block
        [else]
          else block
        [/if]
        [if (false)]
          then block
        [else]
          else block
        [/if]
        [myParam.name/]
      [/protected]
    some text
  [/file]
[/template]